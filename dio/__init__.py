"""dio (DM in one): the GITADORA DrumMania data pipeline -- SQ3/VA3/mdb parsing, instrument semantics,
grid IR and the manic.sqlite builder. Chart-domain only: no torch, no audio decoding (audio lives in
the render module, milestone f2)."""
