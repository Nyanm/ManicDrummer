"""md: the model-side library of ManicDrummer -- audio feature encoding (MuQ), beat-grid alignment,
and (from f4 on) datasets, heads and evaluation. Everything that may touch torch lives here;
chart-domain code stays in dio."""
